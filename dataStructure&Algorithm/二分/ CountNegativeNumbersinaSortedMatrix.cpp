class Solution {
public:
    int countNegatives(vector<vector<int>>& grid) {
        if (grid.size() == 0) {
          return 0;
        }
        int maxY = grid.size(), maxX = grid[0].size();
        int y = maxY/2, x = maxX/2;
        while ( y && x <= maxX ) {

        }
    }
};